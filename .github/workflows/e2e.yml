name: E2E tests

# Disabled 2026-06-22: campaign.spec.js and contributor.spec.js fail at
# import time because SUPABASE_URL / SUPABASE_SERVICE_ROLE_KEY secrets were
# never added to this repo, causing a failure email on every push. Revisit
# scope (full secrets vs. smoke-tests-only) before re-enabling the push
# trigger. workflow_dispatch still works for manual runs once that's decided.
on:
  workflow_dispatch:

jobs:
  e2e:
    runs-on: ubuntu-latest
    timeout-minutes: 20

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Install Playwright browsers
        run: npx playwright install chromium --with-deps

      # Wait for Vercel to finish deploying dev before running tests.
      # The Vercel CLI checks deployment status via the API.
      - name: Wait for dev deployment
        run: |
          npx vercel-deploy-wait --token ${{ secrets.VERCEL_TOKEN }} \
            --team-id ${{ secrets.VERCEL_TEAM_ID }} \
            --project-id ${{ secrets.VERCEL_PROJECT_ID }} \
            --timeout 300 || true
        # "|| true" — if the wait tool isn't configured yet, tests run anyway
        # against whatever is currently live on dev

      - name: Run E2E tests
        run: npx playwright test
        env:
          E2E_BASE_URL:              https://dev.luckysquares.com.au
          SUPABASE_URL:              ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}

      - name: Upload test report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: playwright-report/
          retention-days: 14
