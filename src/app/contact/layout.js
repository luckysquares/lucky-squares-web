export const metadata = {
  title: 'Contact Us',
  description: 'Get in touch with the Lucky Squares Australia support team. Submit a message and receive a ticket reference — we respond within one business day.',
  alternates: { canonical: 'https://luckysquares.com.au/contact' },
};

export default function ContactLayout({ children }) {
  return children;
}
