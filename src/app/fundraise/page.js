import FundraiseApp from '@/components/app/FundraiseApp';

export const metadata = {
  title: 'Fundraiser Dashboard',
  alternates: { canonical: 'https://luckysquares.com.au/fundraise' },
};

export default function FundraisePage() {
  return <FundraiseApp />;
}
